from pathlib import Path


def deploy(config: dict) -> bool:
    """Build the Hugo site, copy output into the output repo, and push.

    This is the top-level orchestration for the deployment step.
    """
    print("deploy: not yet implemented")
    return False
