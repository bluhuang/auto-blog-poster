from pathlib import Path


def build_site(config: dict) -> bool:
    """Run ``hugo`` to build the static site.

    Assumes Hugo is installed and available on PATH.
    Returns True on success, False otherwise.
    """
    print("build_site: not yet implemented")
    return False
