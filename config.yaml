source:
  repo: "blu-obsidian"
  owner: "bluhuang"
  branch: "main"
  notes_subdir: "2 Notes"
  notes_dir: "content/"
  file_pattern: "*.md"

processing:
  image_processing: true
  image_download_dir: "static/images/"
  incremental: true
  cache_file: ".hash_cache.json"
  image_handling:
    enabled: true
    source_images_dir: ""
    target_static_dir: "static/images"
    wiki_image_lookup:
      - type: "same_dir"
      - type: "relative_subdir"
        subdir: "attachments"
  deepseek_api:
    model: "deepseek-v4-flash"
    temperature: 0.3
    max_tokens: 4000
    prompt_template: '请对以下Markdown内容进行脱敏（删除公司名、人名等）、润色语句、删除所有与"面试"相关的内容。只输出处理后的纯Markdown内容，不要添加任何解释、不要添加"好的"、"以下是处理后的内容"等前缀或后缀，直接输出结果。\n\n原文：\n\n{content}'
    system_prompt: |
      你是一个技术博客编辑助手。请处理以下笔记内容：

      1. 删除或替换公司名称、项目代号、同事姓名、联系方式等敏感信息。
      2. 删除所有与"面试"、"面试准备"、"面经"相关的内容（包括段落、句子、关键词）。
      3. 修正语法和拼写错误，优化表达流畅性，但不改变技术事实、代码逻辑和数据。
      4. 保留代码块、数学公式、链接。
      5. 只输出处理后的 Markdown，不要额外解释。

output:
  repo: "blogs-of-bluhuang"
  owner: "bluhuang"
  branch: "gh-pages"
  content_dir: "content/"
  static_dir: "static/"

error:
  failed_log: "failed_notes.log"
  max_retries: 3

hugo:
  executable: "hugo"
  build_options: "--minify"
  base_url: "https://bluhuang.github.io/blogs-of-bluhuang/"
  theme: "PaperMod"
  config_file: "hugo.toml"

deployment:
  target_repo: "bluhuang/blogs-of-bluhuang"
  target_branch: "gh-pages"
  commit_message: "Auto deploy from blog poster [skip ci]"
